# 🌟 CAPTIONCRAFT ENVIRONMENT VARIABLES
# Copy this file to .env and fill in your values

# =============================================================================
# 🗄️ DATABASE CONFIGURATION (REQUIRED)
# =============================================================================
# MongoDB connection string - Use MongoDB Atlas for production
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/captioncraft

# =============================================================================
# 🔐 AUTHENTICATION & SECURITY (REQUIRED)
# =============================================================================
# NextAuth secret - Generate with: openssl rand -hex 32
NEXTAUTH_SECRET=your-super-secret-key-here

# Your application URL
NEXTAUTH_URL=https://ai-caption-generator-pied.vercel.app

# =============================================================================
# 🤖 AI INTEGRATION (REQUIRED)
# =============================================================================
# Google Gemini API key from https://makersuite.google.com/app/apikey
GOOGLE_GENAI_API_KEY=your-gemini-api-key-here

# =============================================================================
# 🖼️ IMAGE STORAGE (REQUIRED)
# =============================================================================
# ImageKit configuration for image uploads
IMAGEKIT_PUBLIC_KEY=your-imagekit-public-key
IMAGEKIT_PRIVATE_KEY=your-imagekit-private-key
IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your-endpoint

# =============================================================================
# 🔒 ADMIN SETUP SECURITY (CRITICAL FOR PRODUCTION)
# =============================================================================
# ⚠️ WARNING: Set to 'true' ONLY when setting up admin for the first time
# ⚠️ NEVER leave this as 'true' in production
ALLOW_ADMIN_SETUP=false

# Generate a strong random token: openssl rand -hex 32
ADMIN_SETUP_TOKEN=your-secure-setup-token-here

# Trusted IPs for admin setup (comma-separated, optional)
TRUSTED_IPS=127.0.0.1,::1,localhost

# =============================================================================
# 📧 EMAIL CONFIGURATION (OPTIONAL BUT RECOMMENDED)
# =============================================================================
# SMTP server configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com

# Alternative email providers:
# Gmail: smtp.gmail.com:587
# Outlook: smtp-mail.outlook.com:587
# Brevo: smtp-relay.sendinblue.com:587

# =============================================================================
# 🔗 GOOGLE OAUTH (OPTIONAL - ENHANCED USER LOGIN)
# =============================================================================
# Google OAuth credentials for social login
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# 🌍 ENVIRONMENT & DEPLOYMENT
# =============================================================================
# Environment mode
NODE_ENV=development

# Public app URL (for production)
NEXT_PUBLIC_APP_URL=https://ai-caption-generator-pied.vercel.app

# =============================================================================
# 📊 MONITORING & ANALYTICS (OPTIONAL)
# =============================================================================
# Sentry for error tracking
SENTRY_DSN=your-sentry-dsn

# Google Analytics
NEXT_PUBLIC_GA_ID=your-ga-id

# =============================================================================
# 🔒 ADDITIONAL SECURITY (OPTIONAL)
# =============================================================================
# Rate limiting configuration
MAX_REQUESTS_PER_MINUTE=100
MAX_REQUESTS_PER_HOUR=1000

# Session configuration
SESSION_MAX_AGE=604800  # 7 days in seconds
SESSION_UPDATE_AGE=86400 # 24 hours in seconds

# =============================================================================
# 🚀 PERFORMANCE OPTIMIZATION (OPTIONAL)
# =============================================================================
# Redis for caching (if using)
REDIS_URL=redis://localhost:6379

# CDN configuration
NEXT_PUBLIC_CDN_URL=https://your-cdn.com

# =============================================================================
# 📝 NOTES & TIPS
# =============================================================================
# 1. Never commit .env files to version control
# 2. Use different values for development and production
# 3. Generate strong secrets for production
# 4. Test all configurations before deploying
# 5. Keep backup of your environment variables
# 6. Rotate secrets regularly in production

# =============================================================================
# 🧪 DEVELOPMENT ONLY VARIABLES
# =============================================================================
# These are automatically set in development
# NODE_ENV=development
# NEXTAUTH_URL=http://localhost:9002
